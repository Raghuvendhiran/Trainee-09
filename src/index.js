import React from 'react';
import ReactDOM from 'react-dom/client';
import BootCard from './/Tasks//1sttask-Card//index';




const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BootCard />
  </React.StrictMode>
);


